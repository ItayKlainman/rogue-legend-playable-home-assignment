import type { WeaponConfig } from '@shared/SpineCharacter';

import spriteData from 'assets/Weapons/DeadeyeBlade.webp';

export const DEADEYES_BLADE: WeaponConfig = {
  spriteData,
  position: { x: 3.56, y: 0.36 },
  rotation: -39.7,
  scale: 1.55,
};
