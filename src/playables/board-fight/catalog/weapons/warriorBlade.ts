import type { WeaponConfig } from '@shared/SpineCharacter';

import swordData from 'assets/Weapons/WarriorBlade.webp';

export const WARRIORS_BLADE: WeaponConfig = {
  spriteData: swordData,
  position: { x: 2.16, y: 0.36 },
  rotation: -39.7,
  scale: 1.25,
};
