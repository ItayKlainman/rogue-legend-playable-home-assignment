import type { WeaponConfig } from '@shared/SpineCharacter';

import spriteData from 'assets/Weapons/DuelistSpear.webp';

export const DUELIST_SPEAR: WeaponConfig = {
  spriteData,
  position: { x: 3.16, y: 0.36 },
  rotation: -39.7,
  scale: 2.00,
};
