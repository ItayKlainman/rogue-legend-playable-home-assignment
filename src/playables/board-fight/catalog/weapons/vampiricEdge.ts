import type { WeaponConfig } from '@shared/SpineCharacter';

import spriteData from 'assets/Weapons/VampiricEdge.webp';

export const VAMPIRIC_EDGE: WeaponConfig = {
  spriteData,
  position: { x: 2.16, y: 0.36 },
  rotation: -39.7,
  scale: 1.25,
};
