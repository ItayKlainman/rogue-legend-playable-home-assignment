import type { WeaponConfig } from '@shared/SpineCharacter';

import spriteData from 'assets/Weapons/SerratedEdge.webp';

export const SERRATED_EDGE: WeaponConfig = {
  spriteData,
  position: { x: 2.16, y: 0.36 },
  rotation: -39.7,
  scale: 1.55,
};
