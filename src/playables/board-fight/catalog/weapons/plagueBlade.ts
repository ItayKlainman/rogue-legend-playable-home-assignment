import type { WeaponConfig } from '@shared/SpineCharacter';

import spriteData from 'assets/Weapons/PlagueBlade.webp';

export const PLAGUE_BLADE: WeaponConfig = {
  spriteData,
  position: { x: 2.16, y: 0.36 },
  rotation: -39.7,
  scale: 1.55,
};
